<div class="invalidFeedback">
        <% errors.forEach(item => { %>
                <% if(item.search("Password") !== -1) { %>
                        <p><%= item %></p>
                <% } %>
        <% }) %>
</div>